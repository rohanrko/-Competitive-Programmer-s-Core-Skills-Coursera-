#include<bits/stdc++.h>
using namespace std

void main() {
    int m;
    cin >> m;
    vector<int> e(m);
    for (int i = 0; i < m; ++i)
        cin >> e[i];

    int result = 0;
    cout << result << endl;

    return 0;
}